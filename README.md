# Tariff Simulator README
To run the simulations, you should use consumers data: annual load profile (1h time-steps) and annual consumption profile (1h time-steps). Which must be stored in a .dat file.
